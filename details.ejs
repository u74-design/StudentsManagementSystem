<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Student Details</title>
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Montserrat:wght@600&display=swap" rel="stylesheet">
<style>
    body {
        font-family: 'Roboto', sans-serif;
        background: #eef2f7;
        margin: 0;
        padding: 0;
        color: #333;
    }

    .container {
        max-width: 900px;
        margin: 40px auto;
        background: #fff;
        padding: 40px;
        border-radius: 20px;
        box-shadow: 0 8px 25px rgba(0,0,0,0.1);
    }

    h1 {
        text-align: center;
        margin-bottom: 35px;
        font-family: 'Montserrat', sans-serif;
        color: #2c3e50;
    }

    /* Student info box */
    .info-box {
        background: #f0f4f8;
        padding: 25px;
        border-radius: 15px;
        border-left: 6px solid #3498db;
        margin-bottom: 40px;
    }
    .info-box p {
        margin: 8px 0;
        font-size: 17px;
    }
    .info-box strong {
        color: #2980b9;
    }

    /* Attendance Circle */
    .attendance-container {
        text-align: center;
        margin-bottom: 40px;
    }

    .circle {
        width: 160px;
        height: 160px;
        border-radius: 50%;
        background: conic-gradient(#3498db 0% calc(var(--percent) * 1%), #ecf0f1 0);
        display: flex;
        align-items: center;
        justify-content: center;
        margin: auto;
        font-size: 32px;
        font-weight: bold;
        color: #2c3e50;
    }

    /* Marks Table */
    table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
        font-size: 16px;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 5px 20px rgba(0,0,0,0.05);
    }

    th, td {
        padding: 14px;
        text-align: center;
    }

    th {
        background: #3498db;
        color: white;
        text-transform: uppercase;
    }

    tr:nth-child(even) {
        background: #f5f7fa;
    }

    tr:hover {
        background: #d6eaf8;
    }

    /* Button */
    button {
        display: block;
        margin: 30px auto 0;
        padding: 12px 25px;
        font-size: 17px;
        font-weight: bold;
        background: #3498db;
        color: white;
        border: none;
        border-radius: 50px;
        cursor: pointer;
    }

    button:hover {
        background: #2980b9;
    }

    h2 {
        text-align: center;
        margin-bottom: 20px;
        font-family: 'Montserrat', sans-serif;
        color: #2c3e50;
    }
</style>
</head>
<body>

<div class="container">

    <h1>Student Details</h1>

    <div class="info-box">
        <p><strong>Roll No:</strong> <%= result.RollNo %></p>
        <p><strong>Name:</strong> <%= result.name %></p>
        <p><strong>Class:</strong> <%= result.class %></p>
        <p><strong>Age:</strong> <%= result.Age %></p>
        <p><strong>Email:</strong> <%= result.email %></p>
    </div>

    <div class="attendance-container">
        <div class="circle" style="--percent: <%= result.attendance %>">
            <%= result.attendance %>%
        </div>
        <p style="font-size:18px; margin-top:10px; font-weight:bold;">Attendance</p>
    </div>

    <h2>Subject Marks</h2>

    <table>
        <tr>
            <th>Science</th>
            <th>English</th>
            <th>Marathi</th>
            <th>Hindi</th>
            <th>Maths</th>
            <th>Sanskrit</th>
        </tr>
        <tr>
            <td><%= result.marks?.science || '-' %></td>
            <td><%= result.marks?.english || '-' %></td>
            <td><%= result.marks?.marathi || '-' %></td>
            <td><%= result.marks?.hindi || '-' %></td>
            <td><%= result.marks?.maths || '-' %></td>
            <td><%= result.marks?.sanskrit || '-' %></td>
        </tr>
    </table>

    <form action="/home">
        <button>Return Home</button>
    </form>

</div>

</body>
</html>
